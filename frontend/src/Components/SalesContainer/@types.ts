import { ISale } from "../../Providers/CarsContext/@types";

type TSaleContainerProps = {
  saleFounded: ISale;
};

type TModalImage = {
  url: string;
};

export type { TSaleContainerProps, TModalImage };
